# 🚀 运行新交易指南

## ⚠️ 当前问题
模型 ID 无效错误，说明你的 OpenAI API 配置有问题。

## 📝 解决步骤

### 1. 检查你的 `.env` 文件

确保 `.env` 文件包含正确的 API 配置：

```bash
# 查看你的 API 配置
cat .env | grep OPENAI
```

### 2. 可能的 API 配置

#### 选项 A: 使用 OpenAI 官方 API
```bash
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_API_KEY=sk-your-actual-openai-key
```
支持的模型：
- `openai/gpt-4o-mini`
- `openai/gpt-4-turbo`
- `openai/gpt-3.5-turbo`

#### 选项 B: 使用 OpenRouter (推荐)
```bash
OPENAI_API_BASE=https://openrouter.ai/api/v1
OPENAI_API_KEY=sk-or-v1-your-openrouter-key
```
支持的模型：
- `openai/gpt-4o-mini`
- `anthropic/claude-3.7-sonnet`
- `deepseek/deepseek-chat`
- `google/gemini-2.0-flash-exp`

#### 选项 C: 使用国内 API 代理
```bash
OPENAI_API_BASE=https://your-proxy-url/v1
OPENAI_API_KEY=your-proxy-key
```

### 3. 修改配置文件

编辑 `configs/default_config.json`，根据你的 API 选择合适的模型：

```json
{
  "models": [
    {
      "name": "gpt-4o-mini",
      "basemodel": "openai/gpt-4o-mini",
      "signature": "gpt-4o-mini",
      "enabled": true
    }
  ]
}
```

### 4. 运行交易系统

```bash
# 方式 1: 使用 Docker (推荐)
docker compose build
docker compose up -d

# 查看日志
docker logs -f ai-trader-main

# 方式 2: 本地运行
python main.py
```

## 🔍 调试步骤

### 检查 API 是否可用
```bash
# 测试 OpenAI API
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer $OPENAI_API_KEY"

# 测试 OpenRouter API
curl https://openrouter.ai/api/v1/models \
  -H "Authorization: Bearer $OPENAI_API_KEY"
```

### 查看容器日志
```bash
# 查看主应用日志
docker logs ai-trader-main

# 查看所有服务日志
docker compose logs -f
```

### 查看交易结果
```bash
# 查看持仓文件
cat data/agent_data/gpt-4o-mini/position/position.jsonl
```

## 📊 配置说明

### 日期范围
当前配置的交易日期：**2025-10-01 到 2025-11-08**

修改 `configs/default_config.json` 中的 `date_range` 来调整：
```json
"date_range": {
  "init_date": "2025-10-01",
  "end_date": "2025-11-08"
}
```

### 初始资金
默认初始资金：**$10,000**

修改 `agent_config.initial_cash` 来调整。

## ⚡ 快速启动命令

```bash
# 一键启动
./docker-start.sh

# 或者
docker compose up -d && docker logs -f ai-trader-main
```

## 🎯 成功运行的标志

看到以下日志说明运行成功：
```
✅ Loaded 6 MCP tools
✅ Agent initialization completed
📈 Starting trading session: 2025-10-02
🔄 Step 1/30
✅ Trading session completed
```

## ❌ 常见错误

### 1. "invalid model ID"
- 原因：API 不支持该模型
- 解决：更换为支持的模型名称

### 2. "API key invalid"
- 原因：API 密钥错误或过期
- 解决：检查 `.env` 文件中的 `OPENAI_API_KEY`

### 3. "Connection refused"
- 原因：MCP 服务未启动
- 解决：确保所有 MCP 服务容器都在运行

### 4. "No trading days to process"
- 原因：日期范围内没有交易日或数据不足
- 解决：检查 `data/daily_prices_*.json` 文件是否包含该日期的数据
