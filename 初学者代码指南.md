# AI-Trader 初学者代码指南

## 📚 目录
1. [项目概述](#项目概述)
2. [核心概念](#核心概念)
3. [代码结构详解](#代码结构详解)
4. [主要代码文件解析](#主要代码文件解析)
5. [工作流程](#工作流程)
6. [实际例子](#实际例子)

---

## 项目概述

### 🤔 这个项目是做什么的？

想象一下：你给5个不同的AI（人工智能）各10,000美元，让它们在股票市场上竞争，看谁能赚更多钱。这个项目就是这样一个"AI交易竞技场"！

**关键特点：**
- ✅ AI完全自主决策（不需要人类干预）
- ✅ 公平竞争（相同的起始资金、相同的数据）
- ✅ 真实市场数据（纳斯达克100指数的股票）
- ✅ 完整记录（每一笔交易都有记录）

---

## 核心概念

### 1️⃣ 什么是AI Agent（AI代理）？

**通俗理解：** AI Agent就像一个虚拟的股票交易员，它能：
- 📊 查看股票价格
- 📰 搜索市场新闻
- 💰 买入和卖出股票
- 🧮 进行数学计算
- 🤔 自己做决策

**代码中的体现：**
```python
class BaseAgent:
    """基础AI代理类 - 就像一个虚拟交易员"""

    def __init__(self, signature, basemodel, initial_cash=10000):
        self.signature = signature        # AI的名字（比如"GPT-4"）
        self.basemodel = basemodel        # AI使用的模型
        self.initial_cash = initial_cash  # 初始资金：10,000美元
```

### 2️⃣ 什么是MCP工具？

**MCP = Model Context Protocol（模型上下文协议）**

**通俗理解：** 就像给AI配备的"工具箱"，包含：

| 工具名称 | 作用 | 类比 |
|---------|------|-----|
| **tool_trade.py** | 买卖股票 | 就像银行账户的转账功能 |
| **tool_get_price_local.py** | 查询价格 | 就像查看股票行情软件 |
| **tool_jina_search.py** | 搜索信息 | 就像用Google搜索新闻 |
| **tool_math.py** | 数学计算 | 就像计算器 |

### 3️⃣ 什么是JSONL文件？

**JSONL = JSON Lines（每行一个JSON对象）**

这是存储数据的格式，每一行记录一次交易：

```json
{"date": "2025-01-20", "id": 1, "this_action": {"action": "buy", "symbol": "AAPL", "amount": 10}, "positions": {"AAPL": 10, "CASH": 9000}}
{"date": "2025-01-21", "id": 2, "this_action": {"action": "sell", "symbol": "AAPL", "amount": 5}, "positions": {"AAPL": 5, "CASH": 9500}}
```

---

## 代码结构详解

### 📂 项目文件结构

```
AI-Alpha-Trader/
├── main.py                    # 🎯 主程序入口（从这里开始运行）
├── agent/
│   └── base_agent/
│       └── base_agent.py      # 🧠 AI交易员的大脑
├── agent_tools/
│   ├── tool_trade.py          # 💰 买卖工具
│   ├── tool_get_price_local.py # 📊 价格查询工具
│   ├── tool_jina_search.py    # 🔍 信息搜索工具
│   └── tool_math.py            # 🧮 数学计算工具
├── data/
│   ├── agent_data/            # 📝 每个AI的交易记录
│   └── daily_prices_*.json    # 📈 股票价格数据
├── tools/
│   ├── price_tools.py         # 价格处理辅助函数
│   └── general_tools.py       # 通用辅助函数
└── configs/
    └── default_config.json    # ⚙️ 配置文件
```

---

## 主要代码文件解析

### 📄 1. main.py - 主程序入口

**作用：** 这是整个程序的"指挥官"，负责启动和协调所有AI。

#### 关键代码解析

```python
async def main(config_path=None):
    """运行交易实验"""

    # 步骤1: 加载配置文件
    config = load_config(config_path)

    # 步骤2: 获取日期范围
    INIT_DATE = config["date_range"]["init_date"]  # 开始日期
    END_DATE = config["date_range"]["end_date"]    # 结束日期

    # 步骤3: 获取启用的AI模型列表
    enabled_models = [
        model for model in config["models"]
        if model.get("enabled", True)  # 只选择启用的模型
    ]

    # 步骤4: 为每个AI模型创建代理并运行
    for model_config in enabled_models:
        # 创建AI代理
        agent = BaseAgent(
            signature=model_config["signature"],
            basemodel=model_config["basemodel"],
            initial_cash=10000.0
        )

        # 初始化AI（连接工具、准备模型）
        await agent.initialize()

        # 运行整个日期范围的交易
        await agent.run_date_range(INIT_DATE, END_DATE)
```

**流程图：**
```
开始
  ↓
读取配置文件
  ↓
获取日期范围（比如：2025-01-01 到 2025-01-31）
  ↓
获取AI模型列表（比如：GPT-4, Claude）
  ↓
为每个AI创建交易代理
  ↓
运行交易模拟
  ↓
记录结果
  ↓
结束
```

---

### 📄 2. base_agent.py - AI交易员的大脑

这是最核心的文件，定义了AI如何思考和交易。

#### 类结构

```python
class BaseAgent:
    """基础AI代理类"""

    def __init__(self, signature, basemodel, initial_cash=10000):
        """初始化AI交易员"""
        self.signature = signature      # AI的名字
        self.basemodel = basemodel      # AI使用的模型
        self.initial_cash = initial_cash # 初始资金

    async def initialize(self):
        """准备工作：连接工具、初始化模型"""
        # 连接MCP工具（买卖、查价、搜索等）
        self.client = MultiServerMCPClient(self.mcp_config)
        self.tools = await self.client.get_tools()

        # 创建AI模型
        self.model = ChatOpenAI(
            model=self.basemodel,
            base_url=self.openai_base_url,
            api_key=self.openai_api_key
        )

    async def run_trading_session(self, today_date):
        """运行一天的交易"""
        # 给AI发送指令
        user_query = f"请分析并更新今天（{today_date}）的持仓"

        # AI开始思考和行动的循环
        for step in range(self.max_steps):
            # AI调用工具、分析数据、做出决策
            response = await self.agent.ainvoke({"messages": message})

            # 检查AI是否完成交易
            if "停止信号" in response:
                break
```

#### 关键方法详解

##### 1. `register_agent()` - 注册新AI

```python
def register_agent(self):
    """注册新的AI交易员，创建初始账户"""

    # 创建初始持仓（所有股票数量为0，现金为10,000美元）
    init_position = {
        "AAPL": 0,
        "MSFT": 0,
        "GOOGL": 0,
        # ... 其他股票
        "CASH": 10000.0  # 初始现金
    }

    # 写入position.jsonl文件
    with open(self.position_file, "w") as f:
        f.write(json.dumps({
            "date": self.init_date,
            "id": 0,
            "positions": init_position
        }) + "\n")
```

**通俗理解：** 就像在银行开户，给AI一个账户，里面有10,000美元现金，还没有任何股票。

##### 2. `run_trading_session()` - 运行一天的交易

```python
async def run_trading_session(self, today_date):
    """运行单日交易会话"""

    # 步骤1: 给AI发送任务
    user_query = "请分析并更新今天的持仓"

    # 步骤2: AI开始循环思考（最多30步）
    for step in range(30):
        # AI调用工具、分析数据
        response = await self.agent.ainvoke({"messages": message})

        # 步骤3: 检查AI是否决定停止
        if "STOP" in response:
            print("AI完成交易决策")
            break

        # 步骤4: 记录AI的操作
        self._log_message(log_file, response)
```

**流程示例：**
```
第1步: AI说："让我先查询AAPL的价格"
第2步: 工具返回："AAPL价格是150美元"
第3步: AI说："我要买10股AAPL"
第4步: 执行买入操作
第5步: AI说："交易完成，STOP"
```

##### 3. `get_trading_dates()` - 获取交易日期

```python
def get_trading_dates(self, init_date, end_date):
    """获取需要交易的日期列表（只包括工作日）"""

    trading_dates = []
    current_date = init_date

    while current_date <= end_date:
        # 只在工作日交易（周一到周五）
        if current_date.weekday() < 5:  # 0=周一, 4=周五
            trading_dates.append(current_date)
        current_date += timedelta(days=1)

    return trading_dates
```

**示例：**
- 输入：2025-01-01（周三）到 2025-01-07（周二）
- 输出：[2025-01-01, 2025-01-02, 2025-01-03, 2025-01-06, 2025-01-07]
- 跳过：2025-01-04（周六）和 2025-01-05（周日）

---

### 📄 3. tool_trade.py - 买卖工具

这个文件定义了AI如何买卖股票。

#### `buy()` 函数详解

```python
@mcp.tool()
def buy(symbol: str, amount: int) -> Dict[str, Any]:
    """买入股票

    参数:
        symbol: 股票代码，比如 "AAPL"（苹果公司）
        amount: 买入数量，比如 10（买10股）

    返回:
        新的持仓情况（包含所有股票数量和现金余额）
    """

    # 步骤1: 获取当前信息
    signature = get_config_value("SIGNATURE")  # 哪个AI在操作
    today_date = get_config_value("TODAY_DATE")  # 今天的日期

    # 步骤2: 获取当前持仓
    current_position, action_id = get_latest_position(today_date, signature)
    # 示例: {"AAPL": 0, "MSFT": 5, "CASH": 8000}

    # 步骤3: 获取股票价格
    price = get_open_prices(today_date, [symbol])[f'{symbol}_price']
    # 示例: AAPL的价格是 150 美元

    # 步骤4: 检查是否有足够的现金
    total_cost = price * amount  # 总花费 = 价格 × 数量
    cash_left = current_position["CASH"] - total_cost

    if cash_left < 0:
        return {"error": "现金不足！"}

    # 步骤5: 执行买入
    new_position = current_position.copy()
    new_position["CASH"] = cash_left          # 减少现金
    new_position[symbol] += amount            # 增加股票数量

    # 步骤6: 记录交易
    with open(position_file, "a") as f:
        f.write(json.dumps({
            "date": today_date,
            "id": action_id + 1,
            "this_action": {
                "action": "buy",
                "symbol": symbol,
                "amount": amount
            },
            "positions": new_position
        }) + "\n")

    return new_position
```

#### 买入流程图

```
开始买入
    ↓
检查当前持仓和现金
    ↓
查询股票当前价格
    ↓
计算总花费 = 价格 × 数量
    ↓
检查现金是否足够？
    ↓ 是
更新持仓：
- 现金减少
- 股票数量增加
    ↓
记录到文件
    ↓
返回新持仓
    ↓
结束
```

#### 实际例子

**场景：** AI想买10股苹果（AAPL）股票

```python
# 交易前状态
current_position = {
    "AAPL": 0,
    "MSFT": 5,
    "CASH": 10000.0
}

# 假设AAPL价格是150美元
# 调用: buy("AAPL", 10)

# 计算过程：
total_cost = 150 * 10 = 1500  # 总花费
cash_left = 10000 - 1500 = 8500  # 剩余现金

# 交易后状态
new_position = {
    "AAPL": 10,      # 买入了10股
    "MSFT": 5,       # 不变
    "CASH": 8500.0   # 减少了1500
}
```

#### `sell()` 函数详解

```python
@mcp.tool()
def sell(symbol: str, amount: int) -> Dict[str, Any]:
    """卖出股票

    参数:
        symbol: 股票代码
        amount: 卖出数量
    """

    # 步骤1-2: 获取当前信息和持仓（同buy函数）
    signature = get_config_value("SIGNATURE")
    today_date = get_config_value("TODAY_DATE")
    current_position, action_id = get_latest_position(today_date, signature)

    # 步骤3: 获取股票价格
    price = get_open_prices(today_date, [symbol])[f'{symbol}_price']

    # 步骤4: 检查是否有足够的股票
    if symbol not in current_position:
        return {"error": f"没有持有{symbol}股票！"}

    if current_position[symbol] < amount:
        return {"error": "股票数量不足！"}

    # 步骤5: 执行卖出
    new_position = current_position.copy()
    new_position[symbol] -= amount           # 减少股票数量
    new_position["CASH"] += price * amount   # 增加现金

    # 步骤6: 记录交易
    # ... 同buy函数

    return new_position
```

---

## 工作流程

### 🔄 完整交易流程

让我们用一个具体例子来说明整个系统如何工作：

#### 第1天：2025-01-20

```
1. 系统启动
   ├─ 读取配置：日期范围 2025-01-20 到 2025-01-31
   ├─ 加载AI模型：GPT-4, Claude
   └─ 启动MCP工具服务

2. GPT-4开始交易（2025-01-20）
   ├─ 初始状态：现金 $10,000，无股票
   │
   ├─ AI思考过程：
   │  ├─ 第1步: "让我查询一下AAPL的价格"
   │  │   └─ 调用工具: get_price("AAPL")
   │  │   └─ 结果: $150.50
   │  │
   │  ├─ 第2步: "让我搜索一下苹果的新闻"
   │  │   └─ 调用工具: search("Apple stock news")
   │  │   └─ 结果: "苹果推出新产品，市场反应积极"
   │  │
   │  ├─ 第3步: "根据分析，我决定买入10股AAPL"
   │  │   └─ 调用工具: buy("AAPL", 10)
   │  │   └─ 花费: $1,505
   │  │   └─ 剩余现金: $8,495
   │  │
   │  └─ 第4步: "交易完成，STOP"
   │
   └─ 交易记录保存到：
       data/agent_data/gpt-4/position/position.jsonl

3. Claude开始交易（2025-01-20）
   ├─ 初始状态：现金 $10,000，无股票
   ├─ AI思考过程：（类似GPT-4）
   └─ 交易记录保存到：
       data/agent_data/claude/position/position.jsonl
```

#### 第2天：2025-01-21

```
1. 系统继续运行下一个交易日

2. GPT-4交易（2025-01-21）
   ├─ 读取昨天的持仓：AAPL 10股，现金 $8,495
   ├─ 查询今日价格：AAPL = $155.00（上涨了！）
   ├─ AI决策：继续持有，不操作
   └─ 记录：无交易

3. Claude交易（2025-01-21）
   ├─ 读取昨天的持仓
   ├─ AI决策：...
   └─ 记录
```

### 📊 数据流示意图

```
┌─────────────┐
│ 配置文件    │
│ config.json │
└──────┬──────┘
       │
       ↓
┌──────────────────┐
│   main.py        │ ← 主程序入口
│  （指挥官）      │
└────┬─────────────┘
     │
     ↓
┌──────────────────┐
│   BaseAgent      │ ← AI交易代理
│  （交易员大脑）  │
└────┬─────────────┘
     │
     ├─→ ┌──────────────┐
     │   │  MCP工具箱   │
     │   ├──────────────┤
     │   │ • 买卖工具   │
     │   │ • 价格工具   │
     │   │ • 搜索工具   │
     │   │ • 数学工具   │
     │   └──────────────┘
     │
     └─→ ┌──────────────┐
         │  数据存储    │
         ├──────────────┤
         │ • 交易记录   │
         │ • 持仓信息   │
         │ • 日志文件   │
         └──────────────┘
```

---

## 实际例子

### 例子1：AI的第一次交易

**场景：** GPT-4模型在2025-01-20首次交易

```python
# 1. 初始状态
initial_position = {
    "AAPL": 0,
    "MSFT": 0,
    "GOOGL": 0,
    # ... 100支股票，全部为0
    "CASH": 10000.0
}

# 2. AI的思考过程（简化版）
AI: "今天是2025-01-20，让我开始分析市场"
AI: "首先查询几只热门股票的价格"

# 调用工具
get_price("AAPL")  → 返回: $150.50
get_price("MSFT")  → 返回: $380.20
get_price("GOOGL") → 返回: $140.80

AI: "让我搜索一下这些公司的新闻"

# 调用搜索工具
search("Apple AAPL news") → 返回: "苹果发布新款iPhone，销量预期强劲"
search("Microsoft MSFT news") → 返回: "微软云业务增长放缓"

AI: "基于分析，我决定：
    1. 买入10股AAPL（看好新产品）
    2. 买入5股GOOGL（价格合理）"

# 执行交易
buy("AAPL", 10)
# 花费: $150.50 × 10 = $1,505
# 剩余现金: $10,000 - $1,505 = $8,495

buy("GOOGL", 5)
# 花费: $140.80 × 5 = $704
# 剩余现金: $8,495 - $704 = $7,791

# 3. 最终持仓
final_position = {
    "AAPL": 10,
    "MSFT": 0,
    "GOOGL": 5,
    # ... 其他股票为0
    "CASH": 7791.0
}

# 4. 记录写入文件
# 文件: data/agent_data/gpt-4/position/position.jsonl
{"date": "2025-01-20", "id": 1, "this_action": {"action": "buy", "symbol": "AAPL", "amount": 10}, "positions": {...}}
{"date": "2025-01-20", "id": 2, "this_action": {"action": "buy", "symbol": "GOOGL", "amount": 5}, "positions": {...}}
```

### 例子2：AI的后续交易

**场景：** 第二天（2025-01-21），AI检查并调整持仓

```python
# 1. 读取昨天的持仓
previous_position = {
    "AAPL": 10,
    "GOOGL": 5,
    "CASH": 7791.0
}

# 2. 查询今天的价格
get_price("AAPL")  → 返回: $155.00（上涨了！）
get_price("GOOGL") → 返回: $138.50（下跌了）

# 3. AI的计算
# AAPL: 买入价 $150.50，现价 $155.00，涨幅 +3%
# GOOGL: 买入价 $140.80，现价 $138.50，跌幅 -1.6%

AI: "AAPL表现不错，继续持有"
AI: "GOOGL小幅下跌，但没有重大负面新闻，继续持有"
AI: "今天不进行任何操作"

# 4. 记录
# 文件中会添加一条"无交易"记录
{"date": "2025-01-21", "id": 3, "this_action": null, "positions": {...}}
```

### 例子3：多个AI的竞争

```python
# 2025-01-20 结束时的对比

# GPT-4的选择
gpt4_position = {
    "AAPL": 10,
    "GOOGL": 5,
    "CASH": 7791.0
}
# 持仓价值 = 10×$155 + 5×$138.5 + $7791 = $9,934 😞（亏损）

# Claude的选择
claude_position = {
    "NVDA": 20,    # 英伟达（AI芯片公司）
    "MSFT": 3,
    "CASH": 5000.0
}
# 持仓价值 = 20×$520 + 3×$385 + $5000 = $16,555 🎉（盈利！）

# Claude在第一天表现更好！
```

---

## 💡 关键概念总结

### 1. 异步编程（async/await）

```python
async def initialize(self):
    """异步初始化"""
    # async 表示这是一个异步函数
    # 可以在执行时"暂停"等待其他操作完成

    self.tools = await self.client.get_tools()
    # await 表示"等待这个操作完成"
```

**通俗理解：**
- 同步（普通）编程：像排队买咖啡，一个人买完，下一个人才能买
- 异步编程：像在餐厅点餐，点完餐后可以做其他事，等餐好了再吃

### 2. 配置文件（JSON）

```json
{
  "agent_type": "BaseAgent",
  "date_range": {
    "init_date": "2025-01-01",
    "end_date": "2025-01-31"
  },
  "models": [
    {
      "name": "GPT-4",
      "basemodel": "gpt-4",
      "enabled": true
    }
  ]
}
```

**作用：** 把设置写在文件里，改设置时不需要改代码

### 3. 环境变量（.env）

```bash
OPENAI_API_KEY=your_key_here
ALPHAADVANTAGE_API_KEY=your_key_here
```

**作用：** 把敏感信息（如API密钥）单独存放，不放在代码里

### 4. JSONL文件格式

```
每一行是一个独立的JSON对象
第1行: {"id": 1, "data": "..."}
第2行: {"id": 2, "data": "..."}
第3行: {"id": 3, "data": "..."}
```

**优点：** 可以逐行追加，不需要读取整个文件

---

## 🎯 学习路径建议

### 初学者学习顺序：

1. **第一步：理解基本概念**
   - 什么是AI Agent
   - 什么是股票交易
   - 什么是JSON格式

2. **第二步：阅读配置文件**
   - 📄 `configs/default_config.json`
   - 理解如何配置AI模型和日期

3. **第三步：理解数据流**
   - 📄 `tool_trade.py` - 看AI如何买卖
   - 📄 `tool_get_price_local.py` - 看AI如何查价

4. **第四步：理解AI逻辑**
   - 📄 `base_agent.py` - 看AI如何思考
   - 理解决策循环

5. **第五步：运行实验**
   - 配置环境变量
   - 运行 `python main.py`
   - 观察结果

6. **第六步：分析结果**
   - 查看 `data/agent_data/` 下的交易记录
   - 对比不同AI的表现

---

## 📚 术语表

| 术语 | 英文 | 解释 |
|-----|------|-----|
| **AI代理** | AI Agent | 能自主行动的AI程序 |
| **MCP** | Model Context Protocol | AI工具调用的标准协议 |
| **JSONL** | JSON Lines | 每行一个JSON的文件格式 |
| **异步** | Async | 不阻塞的执行方式 |
| **持仓** | Position | 当前拥有的股票和现金 |
| **回测** | Backtest | 在历史数据上测试策略 |
| **纳斯达克100** | NASDAQ 100 | 美国科技股指数 |
| **开盘价** | Open Price | 股票当天开始交易的价格 |

---

## 🔍 常见问题

### Q1: AI是如何"思考"的？

A: AI通过以下步骤进行决策：
1. 读取当前日期和市场信息
2. 调用工具获取数据（价格、新闻等）
3. 基于训练数据分析信息
4. 生成买卖决策
5. 执行交易并记录

### Q2: 为什么要用异步编程？

A: 因为AI需要等待：
- 网络请求（获取价格）
- AI模型计算（生成决策）
- 文件操作（读写记录）

异步编程让等待时间不浪费，提高效率。

### Q3: 数据存在哪里？

A: 所有数据存储在 `data/` 目录：
```
data/
├── agent_data/
│   ├── gpt-4/
│   │   ├── position/position.jsonl  ← 交易记录
│   │   └── log/                     ← 详细日志
│   └── claude/
│       └── ...
└── daily_prices_*.json              ← 股票价格数据
```

### Q4: 如何添加新的AI模型？

A: 在配置文件中添加：
```json
{
  "models": [
    {
      "name": "我的新模型",
      "basemodel": "my-model-name",
      "signature": "my-model",
      "enabled": true
    }
  ]
}
```

---

## 🚀 下一步

1. **运行示例**
   ```bash
   cd AI-Alpha-Trader
   python main.py
   ```

2. **查看结果**
   ```bash
   cd data/agent_data
   cat gpt-4/position/position.jsonl
   ```

3. **修改配置**
   - 改变日期范围
   - 添加新模型
   - 调整初始资金

4. **深入学习**
   - 阅读源代码注释
   - 尝试添加新功能
   - 分析AI的决策逻辑

---

## 📞 获取帮助

如果遇到问题：
1. 查看 `README.md` 文档
2. 检查配置文件是否正确
3. 查看日志文件中的错误信息
4. 在GitHub Issues提问

---

**祝你学习愉快！🎉**
